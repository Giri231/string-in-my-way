#include<bits/stdc++.h>

using namespace std;

void reverse(char a[],int n)
{
    char c;
    for(int i=0;i<n;i++)
    {
    c=a[i];
    a[i]=a[n-1];
    a[n-1]=c;
    n--;
    
    }
    cout<< a;
 
  
}

int main()
{
    int n;
    cin>>n;
    char a[n];
    cin>>a;
   reverse(a,n);
   
}


